name: 🚀 20.1.8 Deploy

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      upload:
        type: boolean
        default: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      version: "20.1.8"
      upload: ${{ inputs.upload }}
    secrets: inherit
