{% if context == "host" %}

[settings]
os={{ detect_api.detect_os() }}
arch={{ detect_api.detect_arch() }}
build_type=Release
compiler=clang
compiler.cppstd=26
compiler.libcxx=libc++
compiler.version=22

[tool_requires]
llvm-toolchain/22

{% elif context == "build" %}

[settings]
os={{ detect_api.detect_os() }}
arch={{ detect_api.detect_arch() }}
build_type=Release
compiler=clang
compiler.cppstd=26
compiler.libcxx=libc++
compiler.version=22

[tool_requires]
!llvm-toolchain/*: llvm-toolchain/22, cmake/[>=4.0.0 <5.0.0]

{% endif %}
